# Task: git init hyperchess-live-stream (secret-scrubbed, private repo) + implement move_noise/search_time_cap gaps + redeploy

## Todo Checklist
- [x] Step 1: Audit hyperchess-live-stream for secrets/tokens/API keys before any git operation (filename scan + content pattern scan + manual review of secret-adjacent scripts/templates)
- [x] Step 2: Expand .gitignore for real secrets/build artifacts, git init, stage, commit
- [x] Step 3: Create a private project on the self-hosted GitLab (kyrpy group) and push
- [x] Step 4: Implement SkillProfile.search_time_cap_ms wiring (wall-clock search budget, previously dead metadata)
- [x] Step 5: Implement SkillProfile.move_noise_basis_points wiring (per-level intentional move noise, previously dead metadata)
- [x] Step 6: Test both (unit tests + verify existing suite still green + fmt/clippy clean)
- [x] Step 7: Rebuild server image, redeploy to OpenShift, verify rollout healthy and fix live
- [x] Step 8: Commit + push the wiring fix to the new repo
- [x] Step 9: Report to the user
